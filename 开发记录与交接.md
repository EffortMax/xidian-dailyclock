# 西安电子科技大学 研究生选课脚本 —— 开发记录与交接文档

> 项目路径：`D:\newproject\normal_use\DailyClock_XDU_version-main`
> 运行环境：`D:\newproject\.venv`（Python 3.13.0）
> 文档日期：2026-09-12
> 适用对象：后续接手开发/维护的人

---

## 一、项目现状速览

### 1.1 能做什么

| 功能 | 命令 | 状态 |
|---|---|---|
| 无人值守抢课 | `python courseChoose.py` | ✅ 已真机跑通 |
| 查看可选课程（含校区/来源/空位） | `python courseChoose.py --list [关键字] [--xq 北校区]` | ✅ |
| 查看已选课程 | `python courseChoose.py --list --chosen` | ✅ |
| 退课 | `python courseChoose.py --drop <KCDM或BJDM> [--yes]` | ✅ 接口已实现（未做真实退课测试） |
| 导出课程表 CSV（wake up 可导入） | `python courseQuery.py [--all] [--semester 2026秋]` | ✅ |
| 单独测登录 | `python xidian_login.py [--unattended] [--force] [--diagnose]` | ✅ |
| DES 加密自检 | `python wisedu_des.py` | ✅ 9/9 向量通过 |

### 1.2 文件清单

| 文件 | 行数 | 职责 | 备注 |
|---|---|---|---|
| `courseChoose.py` | 716 | 抢课主程序 | 本轮重写 |
| `xidian_login.py` | 437 | 登录/会话/cookie/验证码 | 本轮新增 |
| `wisedu_des.py` | 231 | 选课系统密码加密（仿 DES） | 本轮新增 |
| `courseQuery.py` | 213 | 导出课程表 CSV | 本轮重写 |
| `config.py` | 17 | 账号与参数 | 用户自行维护 |
| `clock.py` / `utils.py` | 13 / 87 | 原项目的"每日打卡"功能 | **未改动，遗留代码，基本已失效** |
| `test.py` | 49 | 原项目的测试脚本 | 未改动 |
| `requirements.txt` | — | 现在**只需要 `requests`** | 已更新 |
| `.gitignore` | — | 已加入 `config.py`/`cookies.json`/`captcha.jpg`/`course.csv`/`*.log` | 已更新 |
| `cookies.json` | — | 登录态缓存（v2 格式，含 domain/path） | 敏感，勿外传 |
| `course.csv` | — | 导出的课程表 | 生成物 |
| `courseChoose.log` | — | 无人值守日志（带时间戳） | 已 400KB，见"待办" |

### 1.3 当前账号状态（2026-09-12 实测）

- 已选课程 **12 门**，包含 `X1PE2070` 体育、`X1MX0040` 新时代中国特色社会主义理论与实践
  → 这两门正是之前"日志说选上、系统里没有"的课，**修复后已真实选上**，可作为修复有效的实证。
- 本轮可选课程约 **148 个教学班 / 77 个课程代码**（含方案内、方案外等来源）。
- `config.py` 当前目标 `X1TE9015`（通信系统综合实验01，北校区，220/220 已满）。

---

## 二、系统调研结论（最核心的资产，后续开发必读）

原项目基于 2022 年的接口，**整套协议都已变更**。以下是本轮通过抓取站点前端 JS（`courses.js` / `indexjsp.js` / `course.html` / `courseTableFieldDefine.js`）并实测确认的现行协议。

### 2.1 域名与路径（最关键的变化）

| | 2022 老代码 | 现在（2026） |
|---|---|---|
| 选课系统 | `https://yjspt.xidian.edu.cn/yjsxkapp/sys/xsxkapp/` | `https://yjsxk.xidian.edu.cn/yjsxkapp/sys/xsxkapp/` |

- 老域名 `yjspt` 上的 `/yjsxkapp/...` 已被网关废弃：任意子路径都返回
  **HTTP 400 + 230 字节 HTML**（`<title>400 Request Header Or Cookie Too Large`），
  且每次要挂 **25~32 秒**。这是最初 `JSONDecodeError: Expecting value: line 1 column 1 (char 0)` 的来源。
- ⚠️ 陷阱：`yjspt` 的网关会把任何未知 `/gsapp` 路径 **302 到统一身份认证登录页**，
  看起来像"路径存在、只是没登录"，实际登录后是 404。**不能靠 302 判断路径是否有效。**

### 2.2 登录协议（自建登录，不走统一身份认证）

原代码走的是 ids 统一身份认证 SSO + AES 加密密码；**现在系统用自己的登录页**，与 ids 无关。
（ids 那边新加了"拖动图像验证码"，但本项目已完全不需要它。）

页面：`{APP}/index.html`（登录表单 `#loginName` / `#loginPwd` / `#verifyCode` / `#vcodeImg`）

| 步骤 | 请求 | 返回 |
|---|---|---|
| 1. 取验证码 token | `GET {APP}/login/4/vcode.do?timestamp=<ms>` | `{"data":{"token":"..."},"code":"1","msg":"获取验证码成功"}` |
| 2. 取验证码图片 | `GET {APP}/login/vcode/image.do?vtoken=<token>` | `image/jpeg`（约 1600 字节，4 位小写字母数字） |
| 3. 提交登录 | `POST {APP}/login/check/login.do?timestrap=<ms>` | 见下 |
| 4. 判断登录态 | `GET {APP}/xsxkHome/loadPublicInfo_index.do` | `loginUserId` 非空且 ≠ `"fail_user"` 即已登录 |

第 3 步表单字段（**注意是 `timestrap`，站点自己拼错了，不是 timestamp**）：

```
loginName  = 学号
loginPwd   = DES.strEncSimple(密码)        ← 见 2.5
verifyCode = 图片验证码
vtoken     = 第 1 步拿到的 token
```

返回 `code` 语义：

| code | 含义 |
|---|---|
| `"1"` | 登录成功 |
| `"2"` | 用户名或密码不正确 |
| `"3"` | 验证码不正确（换一张重试即可） |
| `"4"` | 在线人数超过上限（等待重试） |

### 2.3 选课协议（**两步式，本项目最大的坑**）

⚠️ **`choiceCourse.do` 返回 `code != 0` 只代表"请求已受理"，不代表选课成功。**
真正的结果必须拿流水号去轮询。这就是"日志说选课成功、系统里却没有"的根因。

```
第一步：POST {APP}/xsxkCourse/choiceCourse.do?_=<ms>
        form body: bjdm=<教学班代码>  lx=<来源>  csrfToken=<...>
        → code == "0"  ：直接失败，msg 是原因（如"你已申请免修当前课程 (#mx01s)"）
        → code != "0"  ：受理成功，msg 是本次请求的流水号 xid    ← 老代码误当成"选课成功"

第二步：POST {APP}/xsxkCourse/loadXkjgRes.do?_=<ms>
        form body: xid=<上一步的 msg>  sfhqdqxkqqs=<第一次 1，之后 0>
        → {"msg": "", "dqxkqqs": 250}      ：还在处理，继续轮询（官方最多 30 次；dqxkqqs ≥ 200 表示高峰期排队数）
        → {"msg": "{\"code\":1,\"msg\":\"选课成功\"}"} ：msg 是 JSON 字符串，解析后 code==1 才算真成功
```

第三步（本项目加的保险）：`GET {APP}/xsxkCourse/loadStdCourseInfo.do` → `results` 里
**能查到该 BJDM 才算数**。接口说成功 ≠ 真成功。

### 2.4 课程来源与 `lx` 参数（**不能写死**）

选课页有多个标签页，各有自己的列表接口，**提交选课时必须带对应的 `lx`**：

| 来源 | 列表接口 | lx |
|---|---|---|
| 方案内课程 | `/xsxkCourse/loadJhnCourseInfo.do` | `0` |
| 公选课 | `/xsxkCourse/loadGxkCourseInfo.do` | `1` |
| 方案外课程 | `/xsxkCourse/loadFanCourseInfo.do` | `2` |
| 跨学科专业课 | `/xsxkCourse/loadKxkzykCourseInfo.do` | `3` |

另有 `/xsxkCourse/loadAllCourseInfo.do`（1078 条）与 `/loadLcAllCourseInfo.do`（本轮全部），
其 `lx` 语义未确认，**暂未接入**。

分页参数：`?query_keyword=&pageIndex=1&pageSize=200&_=<ms>`，`POST`，返回 `{datas:[...], total:N}`。

> ⚠️ 服务器的 `query_keyword` **不索引课程代码**：搜 `X1PE2070` / `X1MX0040` 这种 KCDM 一律 `total=0`，
> 搜班名"羽毛球"也是 0（它只模糊匹配课程名）。**所以必须拉全量、在本地过滤。**

### 2.5 密码加密：`DES.strEncSimple`（**不是标准 DES**）

站点前端：`window.DES={strEncSimple:function(B){return d(B,"1","2","3")}}`。

- 它是"每 4 个字符一块 → 补零成 8 字节 → 依次用密钥 `"1"`/`"2"`/`"3"` 做 3 次 DES" 输出十六进制。
- **与标准 DES 不兼容**：这份 JS 的 PC-2 压缩置换表最后 8 项比标准 DES 少 1：
  - 标准（0 基）：`34,53,46,42,50,36,29,32`
  - 它：`33,52,45,41,49,35,28,31`
- 用 pycryptodome / openssl 的 DES 算出来的密文**对不上**（实测：同输入 `1FAF4D76…` vs `4FC2D2F7…`）。
- 因此必须用本项目逐行移植的 `wisedu_des.py`，其 `SELF_TEST` 里 9 组向量取自**站点 JS 的真实输出**。

### 2.6 其它已验证接口

| 用途 | 接口 | 说明 |
|---|---|---|
| 已选课程 | `GET /xsxkCourse/loadStdCourseInfo.do` | `{"results":[...]}`，权威的"已选"数据源 |
| 课表 | `GET /xsxkCourse/loadKbxx.do` | 含 `xkjgList`(已选) / `results`(每节课) / `rqpkjgallList`(日期化课表) |
| 退课 | `POST /xsxkCourse/cancelCourse.do` | body `bjdm` + `csrfToken`；**这里 `code==1` 才是成功** |
| 退课日志 | `GET /xsxkCourse/loadTkrzxx.do` | `{"results":[...TKSJ 退课时间...]}` |
| 选课公共信息 | `GET /xsxkHome/loadPublicInfo_course.do` | 提供 `csrfToken`、`loginUserId` |
| 系统时间 | `GET /xsxkHome/loadSysdate.do` | 选课开始倒计时用 |

### 2.7 数据字段（课程记录）

关键字段：`KCDM`(课程代码) `KCMC`(课程名) `BJDM`(教学班代码，提交选课用) `BJMC`(教学班名)
`XQMC`/`XQDM`(校区名/码，01=南校区 02=北校区) `KXRS`(容量) `DQRS`(已选人数)
`PKSJDD`(上课时间地点原始串) `PKSJDDMS`(中文串) `RKJS`(任课教师) `XNXQMC`(学期) `XF`(学分) `_lx`/`_source`(本项目加的来源标记)

`PKSJDD` 格式：多段用 `;`（课表接口）或 `<br>`（列表接口）分隔，形如
`"6-17周 星期四[9-11节]J-308"`、`"2-4,6-14周 星期一[5-6节]北校区体育馆"`、`"6周 星期五[3-4节]J-604"`；
**线上/慕课型课程该字段为 `null`**（如"学术规范与论文写作"）。

---

## 三、踩过的坑与根因（时间线）

| # | 表象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `ModuleNotFoundError: No module named 'Crypto'` | venv 里装了 pycryptodome，但目录被无关 PyPI 包 `crypto 1.4.1` 抢成小写 `crypto\`；Python import 大小写敏感 | 删掉 `crypto` 包残留 + 两步重命名目录为 `Crypto` |
| 2 | `SyntaxWarning: invalid escape sequence '\s'` + 正则失效 | 从 JS 直译的 `'/(^\s+)|(\s+$)/g'`：`\s` 非法转义；且 `/`、`g` 在 Python 里是普通字符，该正则永不匹配 | 改成 `r'^\s+|\s+$'` |
| 3 | 选课长时间无响应 | 老域名前缀被网关废弃，每次请求挂 25~32 秒；且**所有 requests 调用都没写 timeout** | 换域名 + 全部加 `TIMEOUT=(6,25)` |
| 4 | `JSONDecodeError: Expecting value: line 1 column 1` | ①请求打到废弃网关返回 HTML；②登录态未建立被重定向；③**该接口会用 `text/html` 返回 JSON**，不能拿 Content-Type 当解析前提 | 只按 body 能否 `json.loads` 判断；新增"系统异常页/登录页/网关 400"识别与可读报错 |
| 5 | 登录成功但"会话复核未登录" | 判断依据用错：`loadStdInfo.do` 不返回 `loginUserId`；站点实际用 `loadPublicInfo_index.do` 的 `loginUserId`（≠`fail_user`） | 改为按站点逻辑判定，并引入三态（已登录/未登录/拿不准） |
| 6 | 长跑后突然全是"系统异常页"（NullPointer） | **`_WEU` 这个持久 cookie 失效时会让服务端抛 NullPointer**（实测：带它 100% 复现，去掉立刻正常） | `_WEU` 不再保存/发送；遇到异常页自动剥离后重试一次并打印"错误标识" |
| 7 | 挂机中途崩栈 `SessionExpired` | `SessionExpired` 继承 `Exception`（顶层只 catch `RuntimeError`），且循环里"监控"那步不在 try 内 | 改继承 `RuntimeError`；把下单+监控合进同一层 try/except；新增 `recover_session()` 自愈 |
| 8 | 登录提示成功、紧接着所有接口 302 | `cookies.json` 只存 `名字:值`，读回来 cookie 挂在空 domain；登录后新 `JSESSIONID` 挂 `domain=yjsxk...`，**两个同名 cookie 并存，旧的可能被发出** | cookie 连 `domain/path/secure/expires` 一起存（v2 格式）；加载/登录前先 `clear()`；旧格式自动忽略并重登 |
| 9 | **日志说"选课成功"但系统里没有** | `choiceCourse.do` 返回 `code!=0` 只代表"已受理"，真结果要轮询 `loadXkjgRes.do`；老代码把 `code==1` 当成成功 | 重写为两步式 + 已选课程复核；`lx` 按来源正确传参 |
| 10 | `courseQuery.py` 崩 `'NoneType' object has no attribute 'split'` | 线上/慕课课 `PKSJDD` 为 `null`，旧代码只判了"字段不存在"没判"值为 None" | 全字段健壮解析；`<br>` 也当分隔符；畸形片段只警告不崩 |

---

## 四、代码结构

### 4.1 `wisedu_des.py` —— 密码加密

- `str_enc_simple(pwd)` / `encrypt_password(pwd)`：等价于站点 `DES.strEncSimple`。
- `SELF_TEST`：9 组真实向量；`python wisedu_des.py` 可自检。
- **改这个文件后务必重跑自检**，否则登录会静默失败。

### 4.2 `xidian_login.py` —— 登录/会话

| 函数 | 作用 |
|---|---|
| `new_session()` | 建带 UA/Referer 的 requests.Session |
| `login_state(session)` | 三态判断：`(True/False/None, 说明)` |
| `fetch_captcha(session)` | 取 `(vtoken, 图片字节)` |
| `make_captcha_provider(unattended)` | 返回验证码回调：ddddocr 自动识别 或 人工输入 |
| `login(session, ...)` | 完成登录；`max_attempts` 次（验证码错自动换图） |
| `ensure_session(...)` | **对外主入口**：优先复用 `cookies.json`，失效则（清空 cookie 后）重登，并做状态复核 |
| `relogin(...)` | 会话过期后重登（默认 5 次尝试，配合 OCR） |
| `save_cookies` / `load_cookies` | v2 格式（含 domain/path），v1 旧格式自动忽略 |
| `describe_session(session)` | `--diagnose` 用：打印各接口原始返回 |
| `strip_transient_cookies` / `is_server_error_page` / `server_error_brief` | `_WEU` 与"系统异常页"处理 |

**关键设计**：`TRANSIENT_COOKIE_NAMES = ("_WEU",)`；会话判定以 `loadPublicInfo_index.do` 的 `loginUserId` 为准。

### 4.3 `courseChoose.py` —— 抢课

| 函数 | 作用 |
|---|---|
| `queryCourseList(...)` | 拉取多个来源的课程并打上 `_lx`/`_source`（默认 4 个来源） |
| `fetchChosenCourses` / `listChosen` / `verifyChosen` | 已选课程查询与**选课结果复核** |
| `filterClasses` / `pickClass` / `matchCampus` / `hasFreeSeat` | 筛选与"优先挑有空位的班" |
| `getCourseInfo(...)` | 按 KCDM（+班名/校区筛选）找到目标教学班 |
| `chooseCourse(...)` | **两步式选课**，返回 `(是否真成功, 说明)` |
| `cancelCourse` / `dropCourse` | 退课（带确认） |
| `try_relogin` / `recover_session` | 会话自愈（失败不抛异常，交给主循环重试） |
| `TeeLogger` | 无人值守日志（每行带时间戳，绝对路径） |
| `main(...)` | 主循环：有空位就下单 → 复核 → 成功退出；满则 `sleep_time` 轮询；课程从列表消失时先查"已选"再判断 |
| `SessionExpired` / `ServerError` | 均继承 `RuntimeError`，顶层兜底能接住 |

### 4.4 `courseQuery.py` —— 课表导出

- `parse_one_item` / `parse_PKSJDD`：健壮解析（`None`、`<br>`、全角空格、单节次、缺地点、脏数据全覆盖）。
- `buildRows`：区分"有排课"与"无排课"（线上课）；`--all` 时可把后者也写进 CSV（备注列标注）。
- CSV 固定写到脚本目录，`utf-8-sig` 编码（Excel 友好）。

---

## 五、配置与命令行

### 5.1 `config.py`

```python
course_KCDM  = "X1TE9015"     # 要抢的课程代码
xqmc_keyword = "北校区"        # 校区筛选：北校区 / 南校区 / 北 / 02 / 多个用逗号
# bjmc_keyword = "03"         # 教学班名称筛选（注释掉=不筛）
sleep_time = 15               # 已满时的轮询间隔（建议 30~60）
unattended = True             # 无人值守：ddddocr 自动识别验证码
log_file = "courseChoose.log" # 无人值守日志
```

### 5.2 常用命令

```powershell
$py  = "D:\newproject\.venv\Scripts\python.exe"
$prj = "D:\newproject\normal_use\DailyClock_XDU_version-main"

& $py "$prj\courseChoose.py"                              # 抢课（无人值守）
& $py "$prj\courseChoose.py" --list                       # 看全部可选课程
& $py "$prj\courseChoose.py" --list 羽毛球 --xq all        # 本地关键字过滤（--xq all 取消校区限制）
& $py "$prj\courseChoose.py" --list --chosen              # 看已选课程
& $py "$prj\courseChoose.py" --drop X1TE9015              # 退课（需输 yes）
& $py "$prj\courseChoose.py" --manual                     # 临时改人工输验证码
& $py "$prj\courseQuery.py" --all                         # 导课程表（含线上课）
& $py "$prj\xidian_login.py" --unattended --force --diagnose   # 单独测登录+诊断
& $py "$prj\wisedu_des.py"                                # DES 自检
```

---

## 六、验证情况（哪些是实测、哪些是 mock）

| 项目 | 验证方式 | 结论 |
|---|---|---|
| DES 加密 | 站点 JS 用 node 跑出的 9 组真实向量 | ✅ 全部一致 |
| 登录全流程 | **真机**：ddddocr 识别 → 登录成功 → 会话复核通过 | ✅ |
| 登录失败分支 | **真机**：假账号 + 错验证码 → `code=3` | ✅ |
| 会话失效自愈 | mock 复刻现场（异常页+失效`_WEU` → 302 → 自动重登 → 继续抢课成功） | ✅ |
| 选课成功分支 | mock 精确复刻站点响应形状（提交→轮询 3 次→`code=1`） | ✅ |
| 选课拒绝分支 | **真机**：英语课返回`你已申请免修当前课程 (#mx01s)` | ✅ |
| 选课结果复核 | mock（未选上=False / 已选上=True） | ✅ |
| **选课真实成功** | **真机间接证据**：`X1PE2070` 体育、`X1MX0040` 政治 已出现在"已选课程"中 | ✅ 修复有效 |
| 已选课程/课表接口 | **真机**：12 门课、17 条上课安排 | ✅ |
| `courseQuery` 解析 | 12 组边界输入单测 + 真机数据 | ✅ |
| 退课 | 仅 mock；真机只测了"不存在的课程"被正确拦截 | ⚠️ **未做真实退课** |
| 多来源列表（公选/方案外/跨学科） | **真机**：方案外 222 条已拉到 | ✅ |

---

## 七、已知限制与风险

1. **验证码靠 OCR，不是 100% 准确**。ddddocr 实测约 4 位小写字母数字，单张 ~10ms；偶有认错（实测出现过一次），
   代码会自动换图重试（登录 3 次 / 重登 5 次）。
2. **会话寿命短**。实测 `cookies.json` 经常几十分钟就失效（也可能被浏览器/手机端登录挤掉，
   系统有"在线人数超过上限"的单会话限制）。挂机期间**不要在其他设备登录同一账号**。
3. **`choiceCourse` 的排队机制**：高峰期 `dqxkqqs ≥ 200`，结果最长要轮询 30 次才出，
   本项目按站点逻辑轮询并如实报告"仍无结果"。
4. **退课未做真实验证**（`cancelCourse` 的 `code==1` 语义来自 JS 源码，逻辑可信但未实测）。
5. **日志轮转已实现**：默认单文件 1 MB、保留 5 份；长期挂机仍应定期检查磁盘使用量。
6. **`clock.py` / `utils.py`（原"每日打卡"）完全未适配**，其接口（`yjspt`/`ids` 那套）早已废弃，
   且其 `requests` 调用也没有 timeout。如无需求可忽略或删除。
7. **CLI 的 `config.py` 里是明文密码**，`cookies.json` 等同密码 —— 已加进 `.gitignore`，务必不要提交/外发。桌面端可选保存密码使用 Windows DPAPI，不写明文密码。
8. `test.py` 是原项目的遗留脚本，未适配新接口。

---

## 八、后续开发建议（按优先级）

1. **退课真机验证**：拿一门确定不要的课走一遍桌面双确认或 `--drop`，确认 `cancelCourse` 的真实语义与最终移除结果。
2. **接入 `loadAllCourseInfo.do`**：现在只接了 4 个有明确 `lx` 的来源；
   若用户要抢的课只出现在"全部课程"里，需要先确认其提交时的 `lx` 取值。
3. **抢课节奏参数化**：`POLL_INTERVAL`（默认 1s）与 `sleep_time` 可合并成一个更直观的配置。
4. **可选：结果通知**（Server酱/邮件/企业微信）—— 无人值守时抢到课能立刻知道。
5. **可选：清理遗留**：删除已失效的 `clock.py`/`utils.py`/`test.py`，或明确标注为历史遗留。

---

## 九、排查手册（下次出问题先看这里）

| 现象 | 先查什么 |
|---|---|
| `No module named 'Crypto'` | 是否又装了 PyPI 的 `crypto` 包把 `Crypto\` 目录顶掉了 |
| 所有接口 302 回 index.html | `cookies.json` 失效 → 跑 `xidian_login.py --diagnose` |
| "系统异常/NullPointer" 页面 | `_WEU` cookie → 代码会自动剥离；把"错误标识"记下来可反馈学校 |
| JSON 解析失败但内容是 JSON | 是不是又把 `Content-Type` 当解析前提了（这套系统会用 `text/html` 返回 JSON） |
| "选课成功"但系统里没有 | 是否只看了 `choiceCourse` 的 `code`、没轮询 `loadXkjgRes`、没复核已选课程 |
| 登录成功但复核未通过 | 是否登录前没清空 cookie（同名 JSESSIONID 冲突） |
| 课程查不到 | ①代码是否在本轮 148 个班里（`--list` 确认）②是否已被选上（已选课程会从可选列表移除）③筛选条件是否太严 |

---

## 十、变更文件一览（相对原始仓库）

| 文件 | 变更 |
|---|---|
| `courseChoose.py` | **重写**（新域名/登录/两步式选课/校区筛选/无人值守/自愈/日志） |
| `courseQuery.py` | **重写**（新登录 + 健壮解析 + CLI） |
| `xidian_login.py` | **新增** |
| `wisedu_des.py` | **新增** |
| `requirements.txt` / `requirements-build.txt` | 桌面、OCR、协议运行依赖与独立 PyInstaller 构建依赖 |
| `.gitignore` | 增加敏感/生成文件 |
| `config.py` | 用户自行维护（新增 `unattended` / `log_file` / `xqmc_keyword`） |
| `README.md` | 重写为现行桌面、CLI、安全与发布说明；历史打卡代码仅标为遗留 |
| `clock.py` / `utils.py` / `test.py` | 未改动（遗留） |

---

## 十一、2026-09-15 续作记录

### 11.1 已完成

- 桌面应用的自动选课从单目标升级为**多目标队列**。
- 每轮只调用一次 `queryCourseList`，同一份课程数据服务所有待处理目标。
- 每个目标独立保存 KCDM、BJDM、教学班筛选、校区筛选与 `on_empty` 策略。
- `on_empty` 支持 `warn` / `ignore_filter` / `abort`；锁定 BJDM 时不会越权放宽。
- 已经选上的目标会从待处理集合移除，全部目标完成后才结束任务。
- 桌面人工验证码改为 Qt 弹窗，不再从后台线程读取终端输入。
- `TeeLogger` 增加启动时与运行中的按大小轮转；默认 1 MB、5 份备份。
- README 已按现行桌面应用和 CLI 行为重写，历史打卡说明移入“遗留代码”说明。

### 11.2 验证

- `python -m unittest discover -v`：15 项测试通过。
- VS Code / 语言服务诊断：0 个 error / warning。
- 新增覆盖：多目标共享单次列表请求、独立筛选放宽、目标策略校验、日志轮转和目标队列 UI。

### 11.3 仍需真实环境确认

- 多目标编排使用 mock 覆盖，尚未在真实选课窗口提交多门课程。
- 人工验证码弹窗已做离屏 UI 冒烟，仍建议人工走一次真实登录。
- 退课真实验证及 `loadAllCourseInfo.do` 的 `lx` 语义仍保持原有待办状态。

---

## 十二、v0.3.0 Windows 发布续作

### 12.1 新增能力

- 登录页增加可选“保存账户和密码”；默认不勾选。
- 密码使用当前 Windows 用户的 DPAPI 加密后写入 `%LOCALAPPDATA%\DailyClockXDU\credentials.json`，不写明文密码。
- 取消勾选并成功登录时会删除已保存凭据。
- 桌面课程页增加精确 BJDM 退课，只有连续两次选择确认后才创建退课任务。
- 退课接口返回成功后最多复核 5 次已选课程；BJDM 仍存在时按失败处理。
- 新增 PyInstaller 单文件配置、版本资源、SHA-256 生成脚本与 `--self-test` 发布包自检。

### 12.2 v0.3.0 验证

- `compileall` 通过，单元与离屏 UI 测试 24/24 通过。
- Windows 真机 DPAPI 临时凭据往返通过，文件内未出现明文测试密码。
- PyInstaller 6.16.0 在 Python 3.13 / Windows x64 构建成功。
- 打包 EXE 的 OCR 模型、ONNX Runtime 与 DPAPI `--self-test` 返回 0。
- 打包 EXE 正常保持运行并显示窗口标题“西电研究生选课助手 0.3.0”。
- 真实登录、选课和课表导出沿用用户此前已确认通过的结果；本轮没有执行破坏性的真实退课。

### 12.3 发布物

- 标签：`v0.3.0`
- 文件：`XDU-Course-Assistant-v0.3.0-windows-x64.exe`
- 大小：198,955,776 字节
- SHA-256：`d23a3887dcec1305ad0845d7480e9b079d38d7545151a2014a9441f3e91ecde0`
- 完整发布记录：`docs/release-v0.3.0.md`
